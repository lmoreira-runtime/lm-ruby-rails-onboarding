<p style="color: green"><%= notice %></p>

<%= render @ebook %>
</br>
<div>
  <% if current_user.seller? && @ebook.user == current_user || current_user.admin? %>
    <%= link_to "Edit this ebook", edit_ebook_path(@ebook), class: 'btn btn-primary me-2' %> 
  <% end %>
  <% if current_user.admin? %>
    <%= link_to "See logs", logs_ebook_path(@ebook), class: 'btn btn-secondary' %> 
  <% end %>
  <%= link_to "Back to ebooks", ebooks_path, class: 'btn btn-secondary' %>
  <% if (current_user.buyer? || current_user.seller?) && @ebook.live? && @ebook.user.seller? && @ebook.user != current_user %>
    <%= button_to 'Buy', buy_ebook_ebook_path(ebook), method: :post, class: 'btn btn-warning' %>
  <% end %>
</br></br>
  <% if current_user.seller? && @ebook.user == current_user || current_user.admin? %>
    <%= button_to "Destroy this ebook", @ebook, method: :delete, class: 'btn btn-danger' %>
  <% end %>
</div>
